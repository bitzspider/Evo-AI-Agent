# Ollama Configuration
OLLAMA_HOST=http://localhost:11434
OLLAMA_MODEL=qwen3-latest

# System Behavior
SYSTEM_PROMPT=You are a helpful AI assistant with access to tools. You can: 1. Answer questions directly when you have the knowledge 2. Use the get_current_time tool when users ask about the current time/date. When a user asks about time, date, or "what time is it", use the tool. For other questions, answer directly. Provide clear, accurate, and concise responses. If the user provides any personal information, you should commit it to memory using the system_memory tool.

# Gradio Interface Configuration
GRADIO_HOST=127.0.0.1
GRADIO_PORT=7860
GRADIO_SHARE=false

# Gmail Configuration
GMAIL_USER=rmalmstead@gmail.com
GMAIL_APP_PASSWORD=zqqp xkpw hmwd mgov
NOTIFICATION_EMAIL=ronm@theinspipeline.com

# Development
DEBUG=false 